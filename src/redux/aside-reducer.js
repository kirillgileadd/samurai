let initialState = {
    asideItems: [
        {name: 'Friends', num: '3'},
        {name: 'Group', num: '6'},
    ],
}

const asideReducer = (state = initialState, action) => {

    return state
}

export default asideReducer